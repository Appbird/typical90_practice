fn cin() -> i32{
    let loaded:i32;
    io::stdin()
        .read_line(&mut loaded)
        .expect("Failed to readline");
    return match loaded.trim().parse().expect("Failed to convert to i32");
    
}
fn main() {
    let mut h:i32;
    let mut w:i32;
    io::stdin()
        
    let mut A:i32[][]:
}
